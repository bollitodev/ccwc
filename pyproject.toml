[project]
name = "ccwc"
version = "0.1.0"
description = "Small version of the wc unix tool in python"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "typer>=0.25.1",
    "typing-extensions>=4.15.0",
]

[project.scripts]
ccwc = "ccwc.cli:app"
